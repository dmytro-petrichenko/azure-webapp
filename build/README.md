# Build scripts

Run NUKE from the `build` folder:

```bash
./build.sh <target>
```

```powershell
.\build.ps1 <target>
```

```cmd
build.cmd <target>
```

The scripts expect the repository root to be the parent directory of this folder.
